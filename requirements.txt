# Core dependencies
google-cloud-bigquery
mcp
python-dotenv

# HTTP transport (default mode)
fastapi
uvicorn[standard]

# Optional: Ngrok support for sharing
# Uncomment if you want to use --ngrok flag
pyngrok
