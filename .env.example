# BigQuery MCP Server - Environment Configuration
# Copy this file to .env and customize as needed

# ============================================
# Authentication
# ============================================
# Path to Google Cloud service account JSON file
# If not set, uses Application Default Credentials (ADC)
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json


# ============================================
# Server Configuration
# ============================================
# HTTP server port (default: 8000)
PORT=8000

# Server host binding (default: 0.0.0.0 for all interfaces)
# Use 127.0.0.1 for localhost only
SERVER_HOST=0.0.0.0


# ============================================
# Access Control
# ============================================
# Path to access control configuration JSON file
# Contains allowed_tables, allowed_datasets, and allowed_patterns
# See access-control.example.json for format
ACCESS_CONTROL_FILE=./access-control.json


# ============================================
# Query Limits
# ============================================
# Maximum number of rows that can be returned by any query (default: 10000)
MAX_QUERY_RESULTS=10000

# Maximum bytes that can be billed per query in MB (default: 100)
# Prevents accidentally running expensive queries
MAX_BYTES_BILLED_MB=100

# Default number of rows to return if not specified (default: 1000)
DEFAULT_QUERY_RESULTS=1000


# ============================================
# Ngrok Configuration (Optional)
# ============================================
# Ngrok authentication token (for paid features and persistent URLs)
# Get your token from: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTH_TOKEN=

# Ngrok dashboard port (default: 4040)
NGROK_DASHBOARD_PORT=4040

# Ngrok region (us, eu, ap, au, sa, jp, in) - requires paid plan
# NGROK_REGION=us